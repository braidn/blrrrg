- Nesta::Page.find_articles[0...5].each do |article|
  .hero-unit
    %h1
      %a{:href => "#{article.abspath}"}
        =article.title
    %div.indexarticledate
      Written For:
      =format_date(article.date)
    %section.articles
      = article.summary
